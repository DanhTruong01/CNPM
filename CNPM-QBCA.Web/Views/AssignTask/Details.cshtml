@model QBCA.ViewModels.AssignTaskViewModel
@{
    ViewBag.Title = "Task Details";
    Layout = "~/Views/Shared/_LayoutHome.cshtml";
}

<style>
    .neumorphic-box {
        background: #f2f3f7;
        border-radius: 25px;
        box-shadow: 8px 8px 16px #d1d9e6, -8px -8px 16px #ffffff;
        padding: 40px;
        margin: 60px auto;
        max-width: 700px;
    }

    .detail-item {
        margin-bottom: 20px;
    }

    .label {
        font-weight: 600;
        color: #4F46E5;
    }

    .value {
        font-size: 16px;
        color: #333;
    }
</style>

<div class="neumorphic-box">
    <h3 style="color: #4F46E5; font-weight: bold; margin-bottom: 30px;">Task Details</h3>

    <div class="detail-item">
        <span class="label">Task Title:</span>
        <span class="value">@Model.TaskType</span>
    </div>

    <div class="detail-item">
        <span class="label">Assigned To:</span>
        <span class="value">@Model.AssignedToName</span>
    </div>

    <div class="detail-item">
        <span class="label">Status:</span>
        <span class="value">@Model.Status</span>
    </div>

    <div class="detail-item">
        <span class="label">Deadline:</span>
        <span class="value">@Model.Deadline.ToString("dd/MM/yyyy")</span>
    </div>

    <div class="detail-item">
        <span class="label">Assigned At:</span>
        <span class="value">@Model.AssignedAt.ToString("dd/MM/yyyy")</span>
    </div>

    <a href="@Url.Action("Edit", "AssignTask", new { id = Model.TaskID })" class="btn btn-sm btn-primary">Edit</a>
    <a href="@Url.Action("Index", "AssignTask")" class="btn btn-sm btn-secondary">Back to List</a>
</div>
